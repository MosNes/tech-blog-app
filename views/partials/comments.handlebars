<div>
    {{#each this}}
    <section class="card m-4">
        <div class="card-header p-2">
            <p><strong>{{user.username}}</strong> on {{format_date created_at}}</p>
        </div>
        <div class="card-content">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>